{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "php artisan storage:link --force && php artisan migrate --seed --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "on_failure"
  }
}
